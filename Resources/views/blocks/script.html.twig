{% if hover is defined and hover in ['popover', 'tooltip'] %}
<script>
    <!--
    $(document).ready(function() {
        {% if hover == 'popover' %}
            $('#vic-image-{{ widget.id }}').popover();
        {% elseif hover == 'tooltip' %}
            $('#vic-image-{{ widget.id }}').tooltip();
        {% endif %}
    });
    -->
</script>
{% endif %}
